{
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vitepress": "^1.5.0"
  },
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && vitepress preview docs",
    "preview": "vitepress preview docs",
    "deploy": "gh-pages -d docs/.vitepress/dist"
  }
}